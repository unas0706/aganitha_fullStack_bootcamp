const user: { name: string; age: number; active: boolean } = {
  name: "John",
  age: 30,
  active: true,
};

// user.age = "thirty";

const user2: { name: string; age?: number } = {
  name: "Alice",
};
